@model IEnumerable<PROJECT.Core.Models.ViewModels.GalleryViewModel>
 @using PROJECT.Core.Helpers
@using System.Web

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>


    @*<table class="table">
        <tr>
            @foreach (var item in Model)
            {


                <td>


                    <img src=@Href(ApplicationConstant.UPLOADED_EMPLOYER_GALLERY_PATH + item.FileName) alt="Sample Image" width="200px" />

                    @Html.DisplayFor(modelItem => item.Title)
                    <br />
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })

                </td>




                 

            }
        </tr>

        </table>*@






<div class="row">
    @foreach (var item in Model)
    {

        <div class="col-lg-4">
            <img class="img-circle" src="@Href(ApplicationConstant.UPLOADED_EMPLOYER_GALLERY_PATH + item.FileName)" alt="No Image" width="140" height="140">
            <h2>@item.Title</h2>
            
            <p><a class="btn btn-default" role="button" href="@Url.Action( "Details", new { id = item.Id })">View details »</a></p>
        </div><!-- /.col-lg-4 -->



         

      






    }
    
  
</div>